# Netlify deployment configuration
[build]
  # Since this is a single HTML file, no build step needed
  publish = "."

[[redirects]]
  # Redirect all routes to index.html for SPA behavior
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

