[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18"

# If you have a package-lock.json, Netlify will use npm ci automatically
# If using yarn, uncomment:
# [build]
#   command = "yarn install && yarn build"
